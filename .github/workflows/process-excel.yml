name: Process Excel Files

on:
  pull_request:
    paths:
      - 'input/**/*.xlsx'
      - 'input/**/*.xls'
    types: [opened, synchronize, reopened]

jobs:
  process-excel:
    runs-on: ubuntu-latest
    # Only run if source branch matches process/**
    if: startsWith(github.head_ref, 'process/')
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --no-cache-dir -r requirements.txt

      - name: Process Excel files
        run: |
          python run_processor.py

      - name: Upload processed files
        uses: actions/upload-artifact@v4
        with:
          name: processed-excel-files
          path: output/
          retention-days: 30

      - name: Commit and push processed files
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add output/
          git add input/
          git diff --staged --quiet || git commit -m "Process Excel files: add output and remove input"
          git push origin HEAD:${{ github.head_ref }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
